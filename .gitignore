# OS
.DS_Store
Thumbs.db
Desktop.ini

# Local agent / IDE pointers (machine-specific paths — never publish)
CLAUDE.md
CLAUDE.local.md

# Editors / IDEs
.vscode/
.idea/
.cursor/
*.swp
*.swo
*~

# Node (if adding tooling later)
node_modules/
package-lock.json

# Build artifacts
dist/
*.crx
*.pem
*.zip

# Dev reference files (large HTML snapshots)
teams-chat-example-*.html

# Environment / secrets
.env
.env.*

# Local / agent notes (optional folder name: dev_docs)
dev_docs/

# Chrome extension packages reject some paths; avoid folder names that start/end with
# underscores inside the extension root. Use dev_tests/ and dev_docs/ as named.

# Pseudo-locale (dev-only, not shipped)
_locales/qps-ploc/
